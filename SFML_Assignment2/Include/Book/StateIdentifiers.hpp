#ifndef BOOK_STATEIDENTIFIERS_HPP
#define BOOK_STATEIDENTIFIERS_HPP


namespace States
{
	enum ID
	{
		None,
		Title,
		Menu,
		Game,
		Loading,
		Pause,
		Settings,
		GameOver,
		level2,	//added so they are recognized
		level3	//added so they are recognized
	};
}

#endif // BOOK_STATEIDENTIFIERS_HPP
